<UserControl
    x:Class="LandBankManagement.Views.CompanyDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LandBankManagement.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:LandBankManagement.Controls"
    xmlns:views="using:LandBankManagement.Views"
    xmlns:viewmodels="using:LandBankManagement.ViewModels"
    mc:Ignorable="d" 
    >

    <UserControl.Resources>
        <x:Double x:Key="CardPictureRadius">132</x:Double>
        <DataTemplate x:Key="DetailsTemplate" x:DataType="viewmodels:CompanyDetailsViewModel" >
            <controls:FluidGrid Margin="0,12,0,0" Columns="1" ColumnSpacing="12" RowSpacing="12">
                <controls:FormTextBox Header="Company Name" Text="{x:Bind EditableItem.Name, Mode=TwoWay}" />
                <controls:FluidGrid Columns="2" ColumnSpacing="6" RowSpacing="12" MinColumnWidth="60">
                    <controls:FormTextBox Header="PAN" Text="{x:Bind EditableItem.PAN, Mode=TwoWay}" />
                    <controls:FormTextBox Header="GSTIN" Text="{x:Bind EditableItem.GSTIN, Mode=TwoWay}" />
                </controls:FluidGrid>
                <controls:FormTextBox Header="EMail Address" Text="{x:Bind EditableItem.Email, Mode=TwoWay}" />
                <controls:FormTextBox Header="Phone" Text="{x:Bind EditableItem.PhoneNo, Mode=TwoWay}" />
                <controls:FormTextBox Header="Address" Text="{x:Bind EditableItem.AddressLine1, Mode=TwoWay}" />

                <controls:FluidGrid Columns="2" ColumnSpacing="6" RowSpacing="12" MinColumnWidth="60">
                    <controls:FormTextBox Header="Postal Code" Text="{x:Bind EditableItem.Pincode, Mode=TwoWay}" />
                    <controls:FormTextBox Header="City" Text="{x:Bind EditableItem.City, Mode=TwoWay}" />
                </controls:FluidGrid>
                
            </controls:FluidGrid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid ColumnSpacing="8" 
          Visibility="{x:Bind ViewModel.Item.IsEmpty, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
        <Grid.ColumnDefinitions>           
            <ColumnDefinition Width="250*"/>
            <ColumnDefinition Width="300*"/>
        </Grid.ColumnDefinitions>

        

        <controls:Details x:Name="details" Grid.Column="0" Margin="20,20,10,20"
                          DetailsContent="{x:Bind ViewModel}"
                          DetailsTemplate="{StaticResource DetailsTemplate}"
                          IsEditMode="{x:Bind ViewModel.IsEditMode, Mode=OneWay}"
                          EditCommand="{x:Bind ViewModel.EditCommand}"
                          DeleteCommand="{x:Bind ViewModel.DeleteCommand}"
                          SaveCommand="{x:Bind ViewModel.SaveCommand}"
                          CancelCommand="{x:Bind ViewModel.CancelCommand}" />
        <StackPanel  Grid.Column="1">
            <Button x:Name="uploadBtn"  Command="{x:Bind ViewModel.EditPictureCommand}" Margin="20 40 0 0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon FontFamily="Segoe MDL2 Assets"  Glyph="&#xE898;" Foreground="#fff" Margin="0 0 7 0"/>
                    <TextBlock  x:Name="uploadName" Text="Upload" Foreground="#fff"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <!--<controls:RoundButton Radius="{StaticResource CardPictureRadius}" Grid.Column="1"
                                      BorderThickness="1"
                                      BorderBrush="White"
                                      Background="Transparent"
                                      Command="{x:Bind ViewModel.EditPictureCommand}"
                                      HorizontalAlignment="Center"
                                      Visibility="{x:Bind ViewModel.IsEditMode, Mode=OneWay}">
           
            <Grid Width="{StaticResource CardPictureRadius}" Height="{StaticResource CardPictureRadius}">
                <Rectangle Fill="White" Opacity="0.75" />
                <TextBlock Text="Upload" 
                               FontSize="13" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               Visibility="{x:Bind ViewModel.IsEnabled, Mode=OneWay}" />
            </Grid>
        </controls:RoundButton>-->
    </Grid>
</UserControl>
